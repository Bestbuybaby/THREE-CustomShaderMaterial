interface:
  display_name: "THREE CustomShaderMaterial"
  short_description: "Repo-specific patterns and workflows for THREE-CustomShaderMaterial"
  default_prompt: "Use the THREE-CustomShaderMaterial repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true